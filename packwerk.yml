# See: Setting up the configuration file
# https://github.com/Shopify/packwerk/blob/main/USAGE.md#configuring-packwerk

# List of patterns for folder paths to include
# include:
# - "**/*.{rb,rake,erb}"

# List of patterns for folder paths to exclude
# exclude:
# - "{bin,node_modules,script,tmp,vendor}/**/*"

# Patterns to find package configuration files
package_paths: "**/"

# List of custom associations, if any
# custom_associations:
# - "cache_belongs_to"

# Whether or not you want the cache enabled (disabled by default)
# cache: true

# Where you want the cache to be stored (default below)
# cache_directory: 'tmp/cache/packwerk'

require:
  - packwerk-extensions
# exclude:
#   - packs/base/app/resources/base/crm/deal_resource* # has_one(:brand..) is yelling for no reasons.
#   - packs/base/app/resources/base/crm/property_group_resource* # has_one(:brand..) is yelling for no reasons.
#   - packs/base/app/resources/base/crm/property_resource* # has_one(:brand..) is yelling for no reasons.
#   - packs/base/app/resources/base/crm/property_parcel_resource* # has_one(:address..) is yelling for no reasons.
#   - packs/base/app/resources/base/crm/property_group_parcel_resource* # has_one(:address..) is yelling for no reasons.
#   - packs/service_app_store_reports/lib/tasks/pdf_preview*.rake # Rake tasks for PDF previews - run in dev environment with everything loaded

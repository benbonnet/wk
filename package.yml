# This file represents the root package of the application
# Please validate the configuration using `packwerk validate` (for Rails applications) or running the auto generated
# test case (for non-Rails projects). You can then use `packwerk check` to check your code.

# Change to `true` to turn on dependency checks for this package
enforce_dependencies: strict
# A list of this package's dependencies
# Note that packages in this list require their own `package.yml` file

# dependencies:
#   # only exceptions below
#   #
#   ##
#   # to sync : deal member, deal, property, coops...
#   - "packs/base"
#   - "packs/workspace_app_crm"
#   ##
#   #
#   - "packs/service_app_store" # <- IN SYSTEM BRAND SUBSCRIPTION USAGE
#   - "packs/system_cron"
#   - "packs/service_bodacc_retablissement" # <- ONLY BECAUSE OF MIGRATIONS;
#   - "packs/service_eu_sanction" # <- ONLY BECAUSE OF MIGRATIONS;
#   - "packs/service_gafi" # <- ONLY BECAUSE OF MIGRATIONS;
#   - "packs/service_ofac" # <- ONLY BECAUSE OF MIGRATIONS;
#   ##
#   #
#   # for clear_preprod
#   - "packs/service_genapi"
